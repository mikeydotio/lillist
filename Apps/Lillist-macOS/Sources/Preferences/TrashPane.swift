import SwiftUI
import LillistCore

/// Stub — real implementation lands in Plan 10 Task 9.
struct TrashPane: View {
    var body: some View {
        Form {
            Text("Trash preferences coming soon.")
        }
        .formStyle(.grouped)
    }
}
