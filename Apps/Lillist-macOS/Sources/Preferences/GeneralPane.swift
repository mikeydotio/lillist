import SwiftUI
import LillistCore

/// Stub — real implementation lands in Plan 10 Task 8.
struct GeneralPane: View {
    var body: some View {
        Form {
            Text("General preferences coming soon.")
        }
        .formStyle(.grouped)
    }
}
