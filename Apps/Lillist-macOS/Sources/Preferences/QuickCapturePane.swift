import SwiftUI
import LillistCore

/// Stub — real implementation lands in Plan 10 Task 9.
struct QuickCapturePane: View {
    var body: some View {
        Form {
            Text("Quick Capture preferences coming soon.")
        }
        .formStyle(.grouped)
    }
}
