import SwiftUI
import LillistCore

/// Stub — real implementation lands in Plan 10 Task 9.
struct NotificationsPane: View {
    var body: some View {
        Form {
            Text("Notifications preferences coming soon.")
        }
        .formStyle(.grouped)
    }
}
